name: Create GitHub Release

on:
  workflow_run:
    workflows: ["Tests and Checks"]
    branches: [main]
    types: 
      - completed

jobs:
  create-release:
    uses: obervinov/_templates/.github/workflows/release.yaml@v1.0.11
